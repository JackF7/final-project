CREATE TABLE `ruth1_comments` (
    `id`            INT(11) NOT NULL AUTO_INCREMENT,
    `title`         VARCHAR(254) NOT NULL,
    `description`   VARCHAR(254) NOT NULL,
    PRIMARY KEY (`id`)
);

CREATE TABLE `ruth2_comments` (
    `id`            INT(11) NOT NULL AUTO_INCREMENT,
    `title`         VARCHAR(254) NOT NULL,
    `description`   VARCHAR(254) NOT NULL,
    PRIMARY KEY (`id`)
);

CREATE TABLE `ruth3_comments` (
    `id`            INT(11) NOT NULL AUTO_INCREMENT,
    `title`         VARCHAR(254) NOT NULL,
    `description`   VARCHAR(254) NOT NULL,
    PRIMARY KEY (`id`)
);

CREATE TABLE `ruth4_comments` (
    `id`            INT(11) NOT NULL AUTO_INCREMENT,
    `title`         VARCHAR(254) NOT NULL,
    `description`   VARCHAR(254) NOT NULL,
    PRIMARY KEY (`id`)
);

INSERT INTO ruth1_comments (title, description) VALUES ('Loyalty', 'This is a story of friendship, love, and loyalty');




